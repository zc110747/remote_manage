
# 1. Linux常用命令
```bash
#系统操作
sudo dpkg -i [file].deb         #解压安装deb文件包
sudo apt-get install [file]     #安装文件
sudo apt-get update             #从软件源端更新软件表
sudo apt-get upgrade            #更新当前软件库
sudo chmod [r-x-w] [file]       #修改权限目录，777全部权限， r+w为5


```